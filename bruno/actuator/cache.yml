info:
  name: cache
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL_WITHOUT_API}}/actuator/caches"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
