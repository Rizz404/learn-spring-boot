info:
  name: health
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL_WITHOUT_API}}/actuator/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
