info:
  name: metrics
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL_WITHOUT_API}}/actuator/metrics"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
