info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/categories"
  body:
    type: json
    data: |
      {
        "name": "Electronics",
        "description": "Gadgets and devices"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
