info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/products"
  body:
    type: json
    data: |-
      {
        "name": "product 1",
        "description": "this is description of product 1",
        "price": "1",
        "category": "unknown"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
