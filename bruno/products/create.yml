info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/products"
  body:
    type: json
    data: |
      {
          "name": "Product 1",
          "description": "this is first product",
          "price": 100,
          "category": "new"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
