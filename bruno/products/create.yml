info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/products"
  body:
    type: json
    data: |-
      {
          "name": "iPhone 20 Pro Max",
          "description": "Apple flagship",
          "price": 19999.99,
          "categoryId": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
