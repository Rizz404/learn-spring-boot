info:
  name: delete by id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BASE_URL}}/products/506"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
