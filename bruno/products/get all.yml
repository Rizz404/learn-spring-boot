info:
  name: get all
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/products?page=2&size=100"
  params:
    - name: page
      value: "2"
      type: query
    - name: size
      value: "100"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
