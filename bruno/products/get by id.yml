info:
  name: get by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/products/8"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
