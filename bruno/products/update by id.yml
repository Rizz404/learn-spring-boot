info:
  name: update by id
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{BASE_URL}}/products/8"
  body:
    type: json
    data: |
      {
          "name": "A",
          "description": "",
          "price": -500,
          "category": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
